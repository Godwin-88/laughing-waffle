# Takwimu LMS web — environment template (copy to apps/web/.env.local)
# Used by SERVER-side components (catalogue, course detail).
API_URL=http://localhost:4000
# Used by BROWSER-side code (auth forms, api client).
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WEB_URL=http://localhost:3000